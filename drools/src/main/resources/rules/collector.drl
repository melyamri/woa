package woa.drools

// Sumamos el importe total de los productos
rule "Initial rule"
	salience 20
    when
			//agent: CustomAgent ();
    then
			System.out.println( "Hola" );
			//agent.execute_task(TaskManager.MOVE)

end
